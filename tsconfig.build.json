{
  "extends": "./tsconfig.json",
  "comment": "빌드 산출물에는 src 만 넣는다. test/prisma/scripts 는 타입 검사(tsconfig.json)에는 포함되지만 dist 로 나가지 않는다.",
  "exclude": ["node_modules", "test", "prisma", "scripts", "dist", "eslint.config.mjs", "**/*spec.ts"]
}
